Lecture 1 Notes:

Source code is what the coder writes, machine code is binary, and a compiler converts one language to another. 
Code editors like VS Code or any of the JetBrains editors have something called syntax highlighting where it colors codes all the code/keywords. 
The terminal is where you run commands for compiling code and writing. 
A graphical user interface (GUI) the visuals of the program. 
The terminal is a command line interface (CLI) where you type the commands.
Say in scratch roughly equals print(f).
The f in printf is formatted. \n at the end makes a new line
Escape sequences are special sequences that allow you to represent characters in a string that would otherwise be difficult or impossible to type directly into code. 
Most programming languages are line based. The computer processes each line one at a time
The printf function is in the stdio.h library
manual.cs50.io - CS50’s C documentation
cs50.h - CS50's header 
An operating system is a program that does the fundamental operations on a device.
A popular coding OS is Linux which comes from Unix.
Linux is high performance.
// Linux Terminal Commands:
// cd folder_name   - Change directory (navigate into a folder)
// cp file dest     - Copy a file to a destination
// ls               - List files and folders in current directory
// mkdir name       - Make a new directory (folder)
// mv file dest     - Move (or rename) a file
// rm file          - Remove (delete) a file
// rmdir folder     - Remove an empty directory
